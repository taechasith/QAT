{
  "installCommand": "corepack enable && corepack prepare pnpm@10.29.3 --activate && cd qat-web && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && cd qat-web && pnpm build",
  "outputDirectory": "qat-web/.next",
  "devCommand": "corepack enable && cd qat-web && pnpm dev --port $PORT"
}
